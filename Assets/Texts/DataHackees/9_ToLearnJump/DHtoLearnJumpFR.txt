###New Fragment###
#FragmentTreshold= 10
Passe : À partir de maintenant, certains Passes seront des morceaux de code simples.
Que vous devrez hacker.

Trace : %Trace%

###New Fragment###
#FragmentTreshold= 15
Passe : 
#include <iostream>
#include <string>
using namespace std;

int main() {
    string mdp1 = "tification";
    string mdp2 = "authen";
    cout << "Mot de passe = " << mdp2 << mdp1 << endl;
   
    return 0;
}

###New Fragment###
#FragmentTreshold= 20
Passe : 

<color=#GREY>// Ceci est un commentaire. Un commentaire n'a aucun effet sur le code. Il permet de donner des indications.</color>

<color=#GREY>// Ces 3 lignes ne sont pas importantes à la compréhension du programme.</color>
#include <iostream>
#include <string>
using namespace std;

<color=#GREY>// Le programme commence ici.</color>
int main() {
    <color=#GREY>// Les 2 lignes suivantes sont des chaînes de caractères. Des mots en fait.</color>
    string mdp1 = "tification";
    string mdp2 = "authen";
    <color=#GREY>// Cette ligne semble afficher le mot de passe à l'écran.</color>
    cout << "Mot de passe = " << mdp2 << mdp1 << endl;
   
    <color=#GREY>// Le programme s'arrête ici.</color>
    return 0;
}

###New Fragment###
#FragmentTreshold= 30
Passe :
<color=#GREY>// Nous allons vous donner un peu plus d'informations.</color>

<color=#GREY>// Le programme commence ici.</color>
int main() {
    <color=#GREY>// Le mot de passe semble être constitué de 2 parties.</color>
    string mdp1 = "tification";
    string mdp2 = "authen";
    <color=#GREY>// Qui sont ensuite juxtaposées l'une après l'autre.</color>
    cout << "Mot de passe = " << mdp2 << mdp1 << endl;
    <color=#GREY>// Attention à l'ordre !</color>
   
    <color=#GREY>// Le programme s'arrête ici.</color>
    return 0;
}

###New Fragment###
#FragmentTreshold= 40
Passe :
<color=#GREY>// Nous allons vous donner encore plus d'informations.</color>

<color=#GREY>// Le programme commence ici.</color>
int main() {
    <color=#GREY>// Le mot de passe est entre les guillements "" !</color>
    cout << "Mot de passe = " << "authentification" << endl;
   
    <color=#GREY>// Le programme s'arrête ici.</color>
    return 0;
}

###New Fragment###
#FragmentTreshold= 50
Vous avez réussi ! Félicitation !

Passe : %Passe%
